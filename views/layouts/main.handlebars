<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  <link rel="stylesheet" type="text/css" href="/css/jass.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css">
</head>

<body>
  <header class="col-12 p-3 bg-black text-white">
    <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <p class="h2 align-items-left mb-2 mb-lg-0 text-white text-decoration-none">
          The Tech Blog &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
        </p>

        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
          <li><a href="/" class="nav-link px-2 text-secondary">Home</a></li>
          {{#if loggedIn}}
          <li>
            <a href="/dashboard" class="nav-link px-2 text-white">Dashboard
            </a>
          </li>
          {{/if}}
        </ul>

        <div class="text-end">
          {{#if logged_in}}
          <button type="button" class="createpost btn btn-warning">Create Post</button>
          <button type="button" class="logout btn btn-outline-light me-2">Logout</button>
          {{else}}
          <button type="button" class="login btn btn-outline-light me-2">Login</button>
          <button type="button" class="signup btn btn-outline-light me-2">Sign-up</button>
          {{/if}}
        </div>
      </div>
    </div>
  </header>
  <main>
    <div class="body row">
      <!-- Render the sub layout -->
      {{{ body }}}
    </div>
  </main>
</body>
{{!-- <footer>
  Nicholas Mamberger © Copyright 2023

</footer> --}}
<script src="/js/logout.js">
</script>

</html>